用requests的get请求将搜素用户名后的整个网页爬取下来，get请求的参数主要有：url,params,headers，其中url为请求地址，params选取携带参数，headers是伪标识，有反反爬的作用。
先建立一个基础的url地址，从控制台输入要查询的用户名后，再将url拼接成一个完整的地址，获取到整个页面后，再利用正则表达式获取需要的部分。程序中通过判断抓取到的rank_list¬长度来判断页面是为为空，既所搜索用户是否存在，最后用json模块中的dumps方法将定义的字典转为json。


